{
  "name": "rad-pi",
  "version": "1.6.1",
  "type": "module",
  "description": "Default Radicle package for pi: core deterministic tooling",
  "keywords": [
    "pi-package",
    "radicle",
    "pi"
  ],
  "author": "Harry Hudson",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/deanh/rad-pi.git"
  },
  "workspaces": [
    "packages/core",
    "packages/cob",
    "packages/autonomy"
  ],
  "files": [
    "packages/core/",
    "LICENSE",
    "README.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "publish:release": "npm test && npm publish --workspace packages/core --access public && npm publish --workspace packages/cob --access public && npm publish --workspace packages/autonomy --access public && npm publish",
    "publish:release:dry-run": "npm test && npm publish --workspace packages/core --access public --dry-run && npm publish --workspace packages/cob --access public --dry-run && npm publish --workspace packages/autonomy --access public --dry-run && npm publish --dry-run"
  },
  "dependencies": {
    "@rad-pi/core": "1.6.1"
  },
  "peerDependencies": {
    "@mariozechner/pi-ai": "^0.64.0",
    "@earendil-works/pi-coding-agent": "^0.80.2",
    "@sinclair/typebox": "*"
  },
  "pi": {
    "extensions": [
      "./packages/core/extensions"
    ],
    "skills": [
      "./packages/core/skills"
    ]
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0"
  }
}
